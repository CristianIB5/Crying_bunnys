using UnityEngine;

public class QuestManager : MonoBehaviour
{
    // Clase provisional (stub) para el sistema de Misiones (Quests)
    void Start()
    {
        Debug.Log("[QuestManager] Inicializado.");
    }
}
